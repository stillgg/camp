var longestCommonPrefix = function (strs) {
  let pref;
  let index = 0;
  let j = 0;
  for (let i = 0; i < strs.length; i++) {}
};

//дорешать
console.log(longestCommonPrefix(["flower", "flow", "flight"]));

// if (pref === strs[i + 1][index]) {
//     pref += strs[i][index + 1];
//     index++;
//   }

// for (const word of strs) {
//     if (word[0 + 1] !== substr) {
//       return "";
//     } else if (word[0] === substr && strs.length === index) {
//       return substr;
//     }
//     index++;
//   }
